type Theme = 'dark' | 'system' | 'light';

type ImageDetails = {
  width: number;
  height: number;
  quality: number;
  src: string;
};

type ImagesCommonDetails = {
  alt: string;
  sizes: string;
};
